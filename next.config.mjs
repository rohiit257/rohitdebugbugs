/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'encrypted-tbn0.gstatic.com',
          "api.microlink.io",
          "cdn.hashnode.com",
          "res.cloudinary.com",
          "media.licdn.com"
        ],
      },
};

export default nextConfig;
